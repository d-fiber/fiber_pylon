# Changelog

## 1.0.0

- Every section below is written by the CI from the commits between two tags, so a subject
  that lands here is the subject that was committed. The version lives in `pubspec.yaml` and
  nowhere else; moving it is what makes the CI write a section, tag the commit and, once the
  owner promotes it, publish a release.
