name: pylon_example
description: "Three backends behind one contract, and a console script that cannot tell them apart."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  fiber_pylon:
    path: ../
  http: ^1.6.0
  equatable: ^3.0.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
